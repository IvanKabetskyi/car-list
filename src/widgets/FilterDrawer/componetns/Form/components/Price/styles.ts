import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    wrapper: {
        flexDirection: 'row',
        flexWrap: 'wrap',
        justifyContent: 'space-between',
        marginTop: 8,
    },
    title: {
        width: '100%',
    },
    inputWrapper: {
        width: '48%',
    },

    helperText: {
        fontSize: 16,
    },
});
