import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    icon: {
        position: 'absolute',
        top: 0,
        right: 0,
    },
    wrapper: {
        height: 20,
    },
});
